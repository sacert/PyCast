extern void print_int(int);
class Test {
  int main() {
    int x;
    for(x = 0; x < 5; x = x + 1) {
      print_int(x);
    }
    for(x = 0; x < 0; x = x + 1) {
      print_int(x);
    }
    for(x = 3; x < 7; x = x + 1) {
      print_int(x);
    }
  }
}
