extern void print_int(int);
class Test {
	int main() {
		int x;
		int y;
		int z;
		x = 0;
		while (x < 100) {
			print_int(x);
			y = 0;
			while (y < 2) {
				print_int(y);
				z = 0;
				while (z < 5) {
					print_int(z);
					z = z + 1;
					if (z == 3) {
						break;
					}
				}
				y = y + 1;
			}
			x = x + 1;
			if (x == 5) {
				break;
			}
		}
	}
}
